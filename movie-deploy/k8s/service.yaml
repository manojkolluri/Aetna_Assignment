apiVersion: v1
kind: Service
metadata:
  name: movie-assistant-service
spec:
  type: LoadBalancer
  selector:
    app: movie-assistant
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
